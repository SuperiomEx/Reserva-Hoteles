import { EstadoReservacionPipe } from './estado-reservacion.pipe';

describe('EstadoReservacionPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoReservacionPipe();
    expect(pipe).toBeTruthy();
  });
});
